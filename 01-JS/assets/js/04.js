//Declaration de plusieurs variables à la suite

var nb1, nb2, resultat;

nb1 = 10;
nb2 = 5;

// -- L'addition' **// 

    // L'addition de nb1 et nb2 avec l'opérateur 

    resultat = nb1+nb2;

    // Affichage du résultat
    console.log("l'addition de nb1/nb2 est égale à :" + resultat);

// -- La soustraction **// 

    // La soustraction de nb1 et nb2 avec l'opérateur 

    resultat = nb1-nb2;

    // Affichage du résultat
    console.log("la soustraction de nb1/nb2 est égale à :" + resultat);

// -- La multiplication **// 

    // La division de nb1 et nb2 avec l'opérateur 

    resultat = nb1*nb2;

    // Affichage du résultat
    console.log("la multiplication de nb1/nb2 est égale à :" + resultat);

// -- La division **// 

    // La division de nb1 et nb2 avec l'opérateur 

    resultat = nb1/nb2;

    // Affichage du résultat
    console.log("la division de nb1/nb2 est égale à :" + resultat);

    // Le modulo //

    // Le resultat du modulo corespond au reste de la division

     // Le mudulo de nb1 et nb2 avec l'opérateur "%""

     nb1 = 11;
     resultat = nb1 % nb2;

     // affichage du résultat dans la console

     console.log ("le modulo de nb1 % nb2 est égal à : " + resultat);


     //// ****** Les écritures simplifiées ******** ///
     
     nb1 =  15;
     nb1 = nb1 + 5;
     console.log(nb1);

     nb1 +=5; // ce qui équivaut à écrire nb1 = nb1 + 5;
     console.log(nb1);

     // Je peux procéder de la même manière pour tous les autres opérateurs arithmétiques